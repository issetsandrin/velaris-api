services:
  api:
    build: .
    image: velaris-api:local
    user: "${UID:-1000}:${GID:-1000}"
    working_dir: /var/www
    volumes:
      - .:/var/www
    command: php artisan serve --host=0.0.0.0 --port=8000
    ports:
      - "8100:8000"
    environment:
      APP_ENV: local
